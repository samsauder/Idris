package com.example.khonsuAndroid.systemsrc

import Benchmark
import Plan

// Stores all benchmarks and plans of the current instance of the system (add tests and tasks later)
class SystemStorage(val benchmarks: List<Benchmark>, val plans: List<Plan>?) { }